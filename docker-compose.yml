version: "3.9"

services:

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rekrutment-backend
    restart: unless-stopped
    ports:
      - "8080:80"   # 🔥 akses langsung via IP
    environment:
      APP_ENV: production
      APP_DEBUG: "false"